  /api/auth/profile:
    get:
      tags:
      - "profile"
      summary: "Returns the information of the logged in user"
      description: "Should return the information of current user"
      produces:
      - "application/json"
      responses:
        "200":
          description: "User info is fetched"
        "401":
          description: "UnauthorizedError: No authorization token was found"