paths:
  /api/auth/google:
    get:
      tags:
      - "OAuth2"
      summary: "Authenticates user using google 3rd party authentication service"
      responses:
        "200":
          description: "Redirects verified userس to the home page"
  